# plotly.py templates (test fixtures)

Copied unmodified from plotly.py's `plotly/package_data/templates/` at commit
`f6707528acbd296590db1526edb829576f35ec80` (https://github.com/plotly/plotly.py, MIT license, © Plotly, Inc.), on
2026-09-24. `plotly-py.test.ts` checks the plotly.py-derived themes in this package against them.
To refresh: download the same files from a newer commit, update the commit above, and fix the
themes until the test passes.
