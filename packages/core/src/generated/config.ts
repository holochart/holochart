// Generated by tools/schema-gen from the attribute schema (plan E1.2). Do not edit by hand.
// Regenerate with `pnpm --filter @mk7s/holochart-schema-gen gen`.

/**
 * Chart configuration (non-visual behavior).
 */
export interface Config {
  /**
   * Resize the chart when its container or the window resizes.
   *
   * @defaultValue `false`
   */
  responsive?: boolean;
  /**
   * Render once with no interactivity (no hover, zoom or modebar).
   *
   * @defaultValue `false`
   */
  staticPlot?: boolean;
  /**
   * Show the modebar always (`true`), never (`false`) or on hover.
   *
   * @defaultValue `"hover"`
   */
  displayModeBar?: 'hover' | true | false;
  /**
   * Names of default modebar buttons to remove.
   *
   * @defaultValue `[]`
   */
  modeBarButtonsToRemove?: readonly string[];
  /**
   * Extra modebar buttons: names of built-in buttons or custom button objects.
   *
   * @defaultValue `[]`
   */
  modeBarButtonsToAdd?: unknown;
  /**
   * Which subplot kinds zoom on mouse wheel. `true`/`false` enable/disable all of them.
   *
   * @defaultValue `"scene+geo+map"`
   */
  scrollZoom?:
    | 'cartesian'
    | 'scene'
    | 'geo'
    | 'map'
    | `${'cartesian' | 'scene' | 'geo' | 'map'}+${string}`
    | true
    | false;
  /**
   * What double-clicking the plot area does.
   *
   * @defaultValue `"reset+autosize"`
   */
  doubleClick?: false | 'reset' | 'autosize' | 'reset+autosize';
  /**
   * Maximum delay between two clicks of a double-click, in milliseconds.
   *
   * Minimum: 0
   *
   * @defaultValue `300`
   */
  doubleClickDelay?: number;
  /**
   * Make titles, legend, annotations and shapes editable in place (E6.7).
   *
   * @defaultValue `false`
   */
  editable?: boolean;
  /**
   * Fine-grained editability, overriding `editable` per element.
   */
  edits?: ConfigEdits;
  /**
   * BCP 47 locale for number/date formatting and UI strings.
   *
   * @defaultValue `"en-US"`
   */
  locale?: string;
  /**
   * Options for the modebar "download image" button.
   */
  toImageButtonOptions?: ConfigToImageButtonOptions;
  /**
   * Renderer pixel ratio. `auto` uses `window.devicePixelRatio`.
   *
   * Range: 0.25 – 8
   *
   * @defaultValue `"auto"`
   */
  pixelRatio?: number | 'auto';
  /**
   * Request an antialiased WebGL context (fixed at context creation).
   *
   * @defaultValue `true`
   */
  antialias?: boolean;
  /**
   * WebGL context power preference hint.
   *
   * @defaultValue `"default"`
   */
  powerPreference?: 'default' | 'high-performance' | 'low-power';
  /**
   * Run the calc stage in a Web Worker (ADR-011). `auto` switches on above a data-size threshold.
   *
   * @defaultValue `false`
   */
  worker?: true | false | 'auto';
  /**
   * Render text as WebGL SDF glyphs or as a DOM overlay (ADR-005).
   *
   * @defaultValue `"webgl"`
   */
  textRenderer?: 'webgl' | 'dom';
  /**
   * Throw on the first validation error instead of warning (once per attribute path).
   *
   * @defaultValue `false`
   */
  strict?: boolean;
  /**
   * Log update plans and pipeline timings to the console.
   *
   * @defaultValue `false`
   */
  debug?: boolean;
}

/**
 * Fine-grained editability, overriding `editable` per element.
 */
export interface ConfigEdits {
  /**
   * Drag annotations.
   *
   * @defaultValue `false`
   */
  annotationPosition?: boolean;
  /**
   * Drag annotation arrow tails.
   *
   * @defaultValue `false`
   */
  annotationTail?: boolean;
  /**
   * Edit annotation text.
   *
   * @defaultValue `false`
   */
  annotationText?: boolean;
  /**
   * Edit axis titles.
   *
   * @defaultValue `false`
   */
  axisTitleText?: boolean;
  /**
   * Drag colorbars.
   *
   * @defaultValue `false`
   */
  colorbarPosition?: boolean;
  /**
   * Edit colorbar titles.
   *
   * @defaultValue `false`
   */
  colorbarTitleText?: boolean;
  /**
   * Drag the legend.
   *
   * @defaultValue `false`
   */
  legendPosition?: boolean;
  /**
   * Edit trace names in the legend.
   *
   * @defaultValue `false`
   */
  legendText?: boolean;
  /**
   * Drag and resize shapes.
   *
   * @defaultValue `false`
   */
  shapePosition?: boolean;
  /**
   * Edit the figure title.
   *
   * @defaultValue `false`
   */
  titleText?: boolean;
}

/**
 * Options for the modebar "download image" button.
 */
export interface ConfigToImageButtonOptions {
  /**
   * Image format.
   *
   * @defaultValue `"png"`
   */
  format?: 'png' | 'jpeg' | 'webp';
  /**
   * File name.
   *
   * @defaultValue `"newplot"`
   */
  filename?: string;
  /**
   * Width in CSS pixels (defaults to the chart).
   *
   * Minimum: 1
   */
  width?: number;
  /**
   * Height in CSS pixels (defaults to the chart).
   *
   * Minimum: 1
   */
  height?: number;
  /**
   * Resolution multiplier.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  scale?: number;
}
