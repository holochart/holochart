// Generated by tools/schema-gen from the attribute schema (plan E1.2). Do not edit by hand.
// Regenerate with `pnpm --filter @mk7s/holochart-schema-gen gen`.

import type { DataArray } from '../schema/types.ts';

/**
 * Figure layout.
 */
export interface Layout {
  /**
   * Figure width in CSS pixels. Ignored when `autosize` fills the container.
   *
   * Minimum: 10
   *
   * @defaultValue `700`
   */
  width?: number;
  /**
   * Figure height in CSS pixels. Ignored when `autosize` fills the container.
   *
   * Minimum: 10
   *
   * @defaultValue `450`
   */
  height?: number;
  /**
   * Size the figure to its container on first render (and on resize with `config.responsive`).
   *
   * @defaultValue `false`
   */
  autosize?: boolean;
  /**
   * Space around the plot area.
   */
  margin?: LayoutMargin;
  /**
   * Background color of the whole figure.
   *
   * @defaultValue `"#fff"`
   */
  paper_bgcolor?: string;
  /**
   * Background color of the plot area between the axes.
   *
   * @defaultValue `"#fff"`
   */
  plot_bgcolor?: string;
  /**
   * Global font. Every text element inherits unset font fields from here.
   */
  font?: LayoutFont;
  /**
   * Default trace colors, cycled by trace index.
   *
   * @defaultValue `["#1f77b4","#ff7f0e","#2ca02c","#d62728","#9467bd","#8c564b","#e377c2","#7f7f7f","#bcbd22","#17becf"]`
   */
  colorway?: readonly string[];
  /**
   * Template: a registered name (`'dark'`), a `+`-composition (`'dark+presentation'`) or an object `{ layout, data }`. `null` disables the default template.
   */
  template?: unknown;
  /**
   * Figure title.
   */
  title?: LayoutTitle;
  /**
   * Show the legend. Defaults to `true` when more than one trace has a legend entry.
   */
  showlegend?: boolean;
  /**
   * Uniform text sizing for bar and pie labels (E4.6).
   */
  uniformtext?: LayoutUniformtext;
  /**
   * How hover picks points: nearest point, all points at the same x/y, or unified labels.
   *
   * @defaultValue `"closest"`
   */
  hovermode?: 'x' | 'y' | 'closest' | false | 'x unified' | 'y unified';
  /**
   * What dragging on the plot area does.
   *
   * @defaultValue `"zoom"`
   */
  dragmode?: 'zoom' | 'pan' | 'select' | 'lasso' | 'orbit' | 'turntable' | false;
  /**
   * Default transition for animated updates (E7.3).
   */
  transition?: LayoutTransition;
  /**
   * While unchanged across updates, user interaction state (zoom, legend toggles, selections) is preserved.
   */
  uirevision?: unknown;
  /**
   * Change this to force data arrays to be re-read when they are mutated in place.
   */
  datarevision?: unknown;
  /**
   * Arbitrary user data, available in text templates as `%{meta}`.
   */
  meta?: unknown;
  /**
   * A grid of subplots (Plotly `layout.grid`). Cartesian axes placed in the grid get their `domain`, `anchor`, `side` and `position` defaults from their cell; domain traces (pie, …) are placed with `domain.row` / `domain.column`. Values given on an axis or a trace always win.
   */
  grid?: LayoutGrid;
  /**
   * A x axis. `xaxis2`, `xaxis3`, … declare further x axes, referenced from traces as `'x2'`, `'x3'`, ….
   */
  xaxis?: LayoutXaxis;
  /**
   * Further `xaxis` containers (`xaxis2`, `xaxis3`, …), with the same attributes.
   */
  [key: `xaxis${number}`]: LayoutXaxis | undefined;
  /**
   * A y axis. `yaxis2`, `yaxis3`, … declare further y axes, referenced from traces as `'y2'`, `'y3'`, ….
   */
  yaxis?: LayoutYaxis;
  /**
   * Further `yaxis` containers (`yaxis2`, `yaxis3`, …), with the same attributes.
   */
  [key: `yaxis${number}`]: LayoutYaxis | undefined;
}

/**
 * Space around the plot area.
 */
export interface LayoutMargin {
  /**
   * Left margin in CSS pixels.
   *
   * Minimum: 0
   *
   * @defaultValue `80`
   */
  l?: number;
  /**
   * Right margin in CSS pixels.
   *
   * Minimum: 0
   *
   * @defaultValue `80`
   */
  r?: number;
  /**
   * Top margin in CSS pixels.
   *
   * Minimum: 0
   *
   * @defaultValue `100`
   */
  t?: number;
  /**
   * Bottom margin in CSS pixels.
   *
   * Minimum: 0
   *
   * @defaultValue `80`
   */
  b?: number;
  /**
   * Padding between the plot area and the axis lines, in CSS pixels.
   *
   * Minimum: 0
   *
   * @defaultValue `0`
   */
  pad?: number;
  /**
   * Let components (legend, colorbars, automargin axes) grow the margins.
   *
   * @defaultValue `true`
   */
  autoexpand?: boolean;
}

/**
 * Global font. Every text element inherits unset font fields from here.
 */
export interface LayoutFont {
  /**
   * CSS font-family list used by every text element unless overridden.
   *
   * @defaultValue `"\"Open Sans\", verdana, arial, sans-serif"`
   */
  family?: string;
  /**
   * Base font size in CSS pixels.
   *
   * Minimum: 1
   *
   * @defaultValue `12`
   */
  size?: number;
  /**
   * Base text color.
   *
   * @defaultValue `"#444"`
   */
  color?: string;
  /**
   * Base font weight.
   *
   * Range: 1 – 1000
   *
   * @defaultValue `"normal"`
   */
  weight?: number | 'normal' | 'bold';
  /**
   * Base font style.
   *
   * @defaultValue `"normal"`
   */
  style?: 'normal' | 'italic';
  /**
   * Capitals variant (CSS `font-variant-caps`). The SDF text renderer approximates small and petite caps with uppercase letters at a reduced size.
   *
   * @defaultValue `"normal"`
   */
  variant?:
    'normal' | 'small-caps' | 'all-small-caps' | 'all-petite-caps' | 'petite-caps' | 'unicase';
  /**
   * Letter case transform: `upper`, `lower`, `word caps` (first letter of each word), or `normal`.
   *
   * @defaultValue `"normal"`
   */
  textcase?: 'normal' | 'word caps' | 'upper' | 'lower';
  /**
   * Text decoration: `under`, `over` and/or `through` joined with `+` (e.g. `under+over`), or `none`.
   *
   * @defaultValue `"none"`
   */
  lineposition?:
    'under' | 'over' | 'through' | `${'under' | 'over' | 'through'}+${string}` | 'none';
  /**
   * CSS `text-shadow` behind the text (`2px 2px 3px black`; only the first shadow is drawn), `none`, or `auto` for a thin halo in the contrast color of the text.
   *
   * @defaultValue `"none"`
   */
  shadow?: string;
}

/**
 * Figure title.
 */
export interface LayoutTitle {
  /**
   * Title text (supports rich-text tags, E2.10).
   *
   * @defaultValue `""`
   */
  text?: string;
  /**
   * Title font. Defaults to `layout.font` with 1.4× size.
   */
  font?: LayoutTitleFont;
  /**
   * Horizontal position in `xref` coordinates.
   *
   * Range: 0 – 1
   *
   * @defaultValue `0.5`
   */
  x?: number;
  /**
   * Vertical position in `yref` coordinates. `auto` places it in the top margin.
   *
   * Range: 0 – 1
   *
   * @defaultValue `"auto"`
   */
  y?: number | 'auto';
  /**
   * `container` spans the whole figure; `paper` spans the plot area.
   *
   * @defaultValue `"container"`
   */
  xref?: 'container' | 'paper';
  /**
   * `container` spans the whole figure; `paper` spans the plot area.
   *
   * @defaultValue `"container"`
   */
  yref?: 'container' | 'paper';
  /**
   * Horizontal alignment of the title box relative to `x`.
   *
   * @defaultValue `"auto"`
   */
  xanchor?: 'auto' | 'left' | 'center' | 'right';
  /**
   * Vertical alignment of the title box relative to `y`.
   *
   * @defaultValue `"auto"`
   */
  yanchor?: 'auto' | 'top' | 'middle' | 'bottom';
}

/**
 * Title font. Defaults to `layout.font` with 1.4× size.
 */
export interface LayoutTitleFont {
  /**
   * CSS font-family list. The renderer uses the first family it can load and falls back through the list.
   */
  family?: string;
  /**
   * Font size in CSS pixels.
   *
   * Minimum: 1
   */
  size?: number;
  /**
   * Text color.
   */
  color?: string;
  /**
   * Font weight: a CSS numeric weight (1–1000), `normal` or `bold`.
   *
   * Range: 1 – 1000
   */
  weight?: number | 'normal' | 'bold';
  /**
   * Font style.
   */
  style?: 'normal' | 'italic';
  /**
   * Capitals variant (CSS `font-variant-caps`). The SDF text renderer approximates small and petite caps with uppercase letters at a reduced size.
   */
  variant?:
    'normal' | 'small-caps' | 'all-small-caps' | 'all-petite-caps' | 'petite-caps' | 'unicase';
  /**
   * Letter case transform: `upper`, `lower`, `word caps` (first letter of each word), or `normal`.
   */
  textcase?: 'normal' | 'word caps' | 'upper' | 'lower';
  /**
   * Text decoration: `under`, `over` and/or `through` joined with `+` (e.g. `under+over`), or `none`.
   */
  lineposition?:
    'under' | 'over' | 'through' | `${'under' | 'over' | 'through'}+${string}` | 'none';
  /**
   * CSS `text-shadow` behind the text (`2px 2px 3px black`; only the first shadow is drawn), `none`, or `auto` for a thin halo in the contrast color of the text.
   */
  shadow?: string;
}

/**
 * Uniform text sizing for bar and pie labels (E4.6).
 */
export interface LayoutUniformtext {
  /**
   * Uniform size for the text inside bars and pie slices, per trace type (E4.6): labels are drawn at the smallest fitted size; `hide` drops labels that would be smaller than `minsize`, `show` draws them at the uniform size. `false` lets every label scale on its own.
   *
   * @defaultValue `false`
   */
  mode?: false | 'hide' | 'show';
  /**
   * Minimum text size in px for `uniformtext.mode`. Also raises trace fonts smaller than it.
   *
   * Minimum: 0
   *
   * @defaultValue `0`
   */
  minsize?: number;
}

/**
 * Default transition for animated updates (E7.3).
 */
export interface LayoutTransition {
  /**
   * Transition duration in milliseconds.
   *
   * Minimum: 0
   *
   * @defaultValue `500`
   */
  duration?: number;
  /**
   * Easing function.
   *
   * @defaultValue `"cubic-in-out"`
   */
  easing?:
    | 'linear'
    | 'quad'
    | 'cubic'
    | 'sin'
    | 'exp'
    | 'circle'
    | 'elastic'
    | 'back'
    | 'bounce'
    | 'linear-in'
    | 'quad-in'
    | 'cubic-in'
    | 'sin-in'
    | 'exp-in'
    | 'circle-in'
    | 'elastic-in'
    | 'back-in'
    | 'bounce-in'
    | 'linear-out'
    | 'quad-out'
    | 'cubic-out'
    | 'sin-out'
    | 'exp-out'
    | 'circle-out'
    | 'elastic-out'
    | 'back-out'
    | 'bounce-out'
    | 'linear-in-out'
    | 'quad-in-out'
    | 'cubic-in-out'
    | 'sin-in-out'
    | 'exp-in-out'
    | 'circle-in-out'
    | 'elastic-in-out'
    | 'back-in-out'
    | 'bounce-in-out';
  /**
   * Whether layout or trace changes animate first.
   *
   * @defaultValue `"layout first"`
   */
  ordering?: 'layout first' | 'traces first';
}

/**
 * A grid of subplots (Plotly `layout.grid`). Cartesian axes placed in the grid get their `domain`, `anchor`, `side` and `position` defaults from their cell; domain traces (pie, …) are placed with `domain.row` / `domain.column`. Values given on an axis or a trace always win.
 */
export interface LayoutGrid {
  /**
   * Number of rows. Defaults to the length of `subplots` or `yaxes`. A grid needs more than one cell (`rows * columns > 1`); otherwise it is ignored.
   *
   * Range: 1 – 100
   */
  rows?: number;
  /**
   * Whether row 0 (of `subplots`, `yaxes` and trace `domain.row`) is the top row or the bottom row.
   *
   * @defaultValue `"top to bottom"`
   */
  roworder?: 'top to bottom' | 'bottom to top';
  /**
   * Number of columns. Defaults to the length of `subplots[0]` or `xaxes`.
   *
   * Range: 1 – 100
   */
  columns?: number;
  /**
   * The cartesian subplot of each cell, as a 2D array `[row][column]` of ids like `'xy'` or `'x2y3'`, or `''` for an empty cell. All cells of a column must share one x axis and all cells of a row one y axis; a subplot that breaks this, or that no trace or axis creates, leaves its cell empty. Takes precedence over `xaxes`/`yaxes`. With `pattern: 'independent'` and no arrays, it defaults to `'xy'`, `'x2y2'`, … in row-major order.
   */
  subplots?: readonly (readonly string[])[];
  /**
   * The x axis of each column (`'x'`, `'x2'`, …, or `''` for none), used when there is no `subplots` array. Defaults to `'x'`, `'x2'`, … for `pattern: 'coupled'`.
   */
  xaxes?: readonly string[];
  /**
   * The y axis of each row (`'y'`, `'y2'`, …, or `''` for none), used when there is no `subplots` array. Defaults to `'y'`, `'y2'`, … for `pattern: 'coupled'`.
   */
  yaxes?: readonly string[];
  /**
   * Default cell contents when neither `subplots` nor `xaxes`/`yaxes` is given: `coupled` shares one x axis per column and one y axis per row (`xaxes: ['x', 'x2', …]`, `yaxes: ['y', 'y2', …]`); `independent` gives every cell its own pair (`'xy'`, `'x2y2'`, … in row-major order).
   *
   * @defaultValue `"coupled"`
   */
  pattern?: 'independent' | 'coupled';
  /**
   * Horizontal space between columns, as a fraction of a column width. Defaults to 0.2 for a grid of independent subplots (`subplots` or `pattern: independent`), 0.1 otherwise.
   *
   * Range: 0 – 1
   */
  xgap?: number;
  /**
   * Vertical space between rows, as a fraction of a row height. Defaults to 0.3 for a grid of independent subplots (`subplots` or `pattern: independent`), 0.1 otherwise.
   *
   * Range: 0 – 1
   */
  ygap?: number;
  /**
   * The part of the plot area the grid fills.
   */
  domain?: LayoutGridDomain;
  /**
   * Where the x axes are drawn by default: `bottom plot` / `top plot` against the bottom-most / top-most subplot of their column; `bottom` / `top` at the bottom / top edge of the grid (`anchor: 'free'`), even when that cell is empty.
   *
   * @defaultValue `"bottom plot"`
   */
  xside?: 'bottom' | 'bottom plot' | 'top plot' | 'top';
  /**
   * Where the y axes are drawn by default: `left plot` / `right plot` against the left-most / right-most subplot of their row; `left` / `right` at the left / right edge of the grid (`anchor: 'free'`), even when that cell is empty.
   *
   * @defaultValue `"left plot"`
   */
  yside?: 'left' | 'left plot' | 'right plot' | 'right';
}

/**
 * The part of the plot area the grid fills.
 */
export interface LayoutGridDomain {
  /**
   * Horizontal extent `[start, end]` of the whole grid, as fractions of the plot area. An empty or reversed extent falls back to `[0, 1]`.
   *
   * @defaultValue `[0,1]`
   */
  x?: readonly [number, number];
  /**
   * Vertical extent `[start, end]` of the whole grid, as fractions of the plot area (from the bottom). An empty or reversed extent falls back to `[0, 1]`.
   *
   * @defaultValue `[0,1]`
   */
  y?: readonly [number, number];
}

/**
 * A x axis. `xaxis2`, `xaxis3`, … declare further x axes, referenced from traces as `'x2'`, `'x3'`, ….
 */
export interface LayoutXaxis {
  /**
   * Draw the axis (line, ticks, labels, grid and title). A hidden axis still maps data to pixels.
   *
   * @defaultValue `true`
   */
  visible?: boolean;
  /**
   * Base color of the axis: the default for `linecolor`, `tickcolor`, `zerolinecolor`, `dividercolor` and (when changed) the tick-label and title font colors.
   *
   * @defaultValue `"#444"`
   */
  color?: string;
  /**
   * Axis type. `'-'` detects the type from the data of the first trace on this axis (Plotly's rules: mostly dates → `date`; more than twice as many distinct strings as numbers → `category`; two-level `[[groups], [items]]` arrays → `multicategory`; otherwise `linear`).
   *
   * @defaultValue `"-"`
   */
  type?: '-' | 'linear' | 'log' | 'date' | 'category' | 'multicategory';
  /**
   * How type detection treats numeric strings: `convert types` counts `'12'` as a number; `strict` counts only real numbers, so numeric strings make a `category` axis.
   *
   * @defaultValue `"convert types"`
   */
  autotypenumbers?: 'convert types' | 'strict';
  /**
   * Whether the range is computed from the data. `reversed` autoranges with the axis flipped. `min`/`max` autorange only that end and take the other from `range` (`min reversed`/`max reversed` also flip the axis). Defaults to `false` when a full `range` is given, and to `min`/`max` when one end of `range` is `null`.
   *
   * @defaultValue `true`
   */
  autorange?: true | false | 'reversed' | 'min reversed' | 'max reversed' | 'min' | 'max';
  /**
   * Constraints applied to the computed autorange.
   */
  autorangeoptions?: LayoutXaxisAutorangeoptions;
  /**
   * Linear axes only. `tozero` extends the autorange to include 0; `nonnegative` keeps it ≥ 0.
   *
   * @defaultValue `"normal"`
   */
  rangemode?: 'normal' | 'tozero' | 'nonnegative';
  /**
   * Disables zoom and pan on this axis (drag, scroll, and modebar zoom leave its range alone).
   *
   * @defaultValue `false`
   */
  fixedrange?: boolean;
  /**
   * Visible range `[start, end]` in data units (numbers, dates or category names; exponents on log axes, as in Plotly). Setting it turns `autorange` off; `null` for one end autoranges that end only.
   */
  range?: readonly [unknown, unknown];
  /**
   * Lowest value zoom and pan may reach, in range units (like `range`: exponents on log axes). Also caps the autorange.
   */
  minallowed?: unknown;
  /**
   * Highest value zoom and pan may reach, in range units (like `range`: exponents on log axes). Also caps the autorange.
   */
  maxallowed?: unknown;
  /**
   * Fraction of the plot area `[start, end]` this axis spans.
   *
   * @defaultValue `[0,1]`
   */
  domain?: readonly [number, number];
  /**
   * The y axis this axis is drawn against, or `free`. Defaults to the y axis of the first subplot using this axis.
   *
   * @defaultValue `"y"`
   */
  anchor?: 'y' | `y${number}` | 'free';
  /**
   * Which side of the plot area the axis is drawn on.
   *
   * @defaultValue `"bottom"`
   */
  side?: 'bottom' | 'top';
  /**
   * Draw this axis over another x axis (`'x'`, `'x2'`, …), sharing its `domain` (this axis' own `domain` is ignored), e.g. a secondary y axis with `side: 'right'`. Unset (or `free`) by default. The target must exist and must not overlay another axis itself; otherwise this is ignored. Templates cannot set it, since it names specific axes. Zoom and pan do not yet move overlaid axes together (plan E3.9).
   *
   * @defaultValue `"x"`
   */
  overlaying?: 'x' | `x${number}` | 'free';
  /**
   * Position of the axis in paper coordinates (0–1 across the y direction). Only used when `anchor` is `free`.
   *
   * Range: 0 – 1
   *
   * @defaultValue `0`
   */
  position?: number;
  /**
   * Draw the axis line, ticks and labels above or below the traces. Grid lines are always below.
   *
   * @defaultValue `"above traces"`
   */
  layer?: 'above traces' | 'below traces';
  /**
   * Grow the figure margins so tick labels and the title fit (E4.2). `true` allows every direction; flags restrict it.
   *
   * @defaultValue `false`
   */
  automargin?:
    | 'height'
    | 'width'
    | 'left'
    | 'right'
    | 'top'
    | 'bottom'
    | `${'height' | 'width' | 'left' | 'right' | 'top' | 'bottom'}+${string}`
    | true
    | false;
  /**
   * `auto` picks up to `nticks` round ticks; `linear` places ticks from `tick0` every `dtick`; `array` uses `tickvals`/`ticktext`; `sync` aligns with the overlaid axis (reserved; behaves as `auto`). Defaults to `array` when `tickvals` is set, `linear` when `dtick` is set, else `auto`.
   */
  tickmode?: 'auto' | 'linear' | 'array' | 'sync';
  /**
   * Maximum number of ticks for `tickmode: auto` (the actual number may be lower so steps stay round). 0 picks it from the axis length.
   *
   * Minimum: 0
   *
   * @defaultValue `0`
   */
  nticks?: number;
  /**
   * Position of one tick for `tickmode: linear` (data units; a date on date axes, an exponent on log axes unless `dtick` is `L<f>`). Defaults to 0, or 2000-01-01 on date axes.
   */
  tick0?: unknown;
  /**
   * Tick step. Linear axes: a positive number. Log axes: an integer `n` (a tick every `n` decades), `L<f>` (linear steps of `f` in data units, e.g. `L0.5`), `D1` (every digit) or `D2` (1, 2 and 5). Date axes: milliseconds, or `M<n>` for `n` months (`M12` for years).
   */
  dtick?: unknown;
  /**
   * Tick positions for `tickmode: array` (data units).
   */
  tickvals?: DataArray;
  /**
   * Tick labels for `tickvals`. Missing entries are formatted as usual.
   */
  ticktext?: DataArray;
  /**
   * Show tick labels.
   *
   * @defaultValue `true`
   */
  showticklabels?: boolean;
  /**
   * Tick-label font. Defaults to `layout.font`.
   */
  tickfont?: LayoutXaxisTickfont;
  /**
   * Tick-label rotation in degrees. `auto` rotates x-axis labels by the first `autotickangles` entry that avoids overlap.
   *
   * @defaultValue `"auto"`
   */
  tickangle?: number | 'auto';
  /**
   * Candidate angles, in order of preference, for `tickangle: auto`.
   *
   * @defaultValue `[0,30,90]`
   */
  autotickangles?: readonly number[];
  /**
   * Tick-label format: a [d3-format](https://github.com/d3/d3-format) specifier for numbers (`.2f`, `~s`, `$,`) or a [d3-time-format](https://github.com/d3/d3-time-format) specifier for dates (`%b %Y`), plus `%{n}f` for `n` digits of fractional seconds and `%h` for the half year. Empty picks a format automatically.
   *
   * @defaultValue `""`
   */
  tickformat?: string;
  /**
   * Zoom-dependent tick formats: the first enabled stop whose `dtickrange` contains the current tick step replaces `tickformat`.
   */
  tickformatstops?: Array<LayoutXaxisTickformatstop>;
  /**
   * Format of this axis’ values in hover labels (same syntax as `tickformat`). Empty uses the tick format with extra precision.
   *
   * @defaultValue `""`
   */
  hoverformat?: string;
  /**
   * Text before each tick label.
   *
   * @defaultValue `""`
   */
  tickprefix?: string;
  /**
   * Which tick labels get `tickprefix`.
   *
   * @defaultValue `"all"`
   */
  showtickprefix?: 'all' | 'first' | 'last' | 'none';
  /**
   * Text after each tick label.
   *
   * @defaultValue `""`
   */
  ticksuffix?: string;
  /**
   * Which tick labels get `ticksuffix`.
   *
   * @defaultValue `"all"`
   */
  showticksuffix?: 'all' | 'first' | 'last' | 'none';
  /**
   * How large and small numbers are written: `none` (1000000), `e` (1e+6), `E` (1E+6), `power` (1×10⁶), `SI` (1M), `B` (like SI but 1B for 10⁹).
   *
   * @defaultValue `"B"`
   */
  exponentformat?: 'none' | 'e' | 'E' | 'power' | 'SI' | 'B';
  /**
   * Which tick labels show the exponent.
   *
   * @defaultValue `"all"`
   */
  showexponent?: 'all' | 'first' | 'last' | 'none';
  /**
   * Smallest decimal exponent written in exponent form (`SI`/`B` and exponent formats); smaller magnitudes are written out in full.
   *
   * Minimum: 0
   *
   * @defaultValue `3`
   */
  minexponent?: number;
  /**
   * Always separate thousands (1,000). By default only numbers with 5 or more integer digits are separated.
   *
   * @defaultValue `false`
   */
  separatethousands?: boolean;
  /**
   * Date axes: `instant` labels the tick instant; `period` centers each label in the period that starts at its tick (e.g. month names between month ticks).
   *
   * @defaultValue `"instant"`
   */
  ticklabelmode?: 'instant' | 'period';
  /**
   * Tick labels outside or inside the plot area, optionally shifted to one side of the tick (`top`/`bottom` for y axes, `left`/`right` for x axes).
   *
   * @defaultValue `"outside"`
   */
  ticklabelposition?:
    | 'outside'
    | 'inside'
    | 'outside top'
    | 'inside top'
    | 'outside left'
    | 'inside left'
    | 'outside right'
    | 'inside right'
    | 'outside bottom'
    | 'inside bottom';
  /**
   * What to do with labels that overflow: keep them, or hide those past the figure or the axis domain. Defaults to `hide past div` for outside labels and `hide past domain` for inside labels.
   */
  ticklabeloverflow?: 'allow' | 'hide past div' | 'hide past domain';
  /**
   * Label every n-th tick only (`tickmode` `auto` and `linear`).
   *
   * Minimum: 1
   *
   * @defaultValue `1`
   */
  ticklabelstep?: number;
  /**
   * Shift of tick labels along the axis, in px.
   *
   * @defaultValue `0`
   */
  ticklabelshift?: number;
  /**
   * Extra distance between tick labels and ticks, in px.
   *
   * @defaultValue `0`
   */
  ticklabelstandoff?: number;
  /**
   * Order of categories: as they first appear in the traces (`trace`), alphabetical/numerical (`category ascending|descending`), `categoryarray` first (`array`), or by an aggregate of the trace values per category (`total`, `min`, `max`, `sum`, `mean`, `median`). Defaults to `array` when `categoryarray` is set, else `trace`.
   */
  categoryorder?:
    | 'trace'
    | 'category ascending'
    | 'category descending'
    | 'array'
    | 'total ascending'
    | 'total descending'
    | 'min ascending'
    | 'min descending'
    | 'max ascending'
    | 'max descending'
    | 'sum ascending'
    | 'sum descending'
    | 'mean ascending'
    | 'mean descending'
    | 'median ascending'
    | 'median descending';
  /**
   * Category order for `categoryorder: array`. Categories not listed follow in trace order.
   */
  categoryarray?: DataArray;
  /**
   * Multicategory axes: draw divider lines between groups.
   *
   * @defaultValue `true`
   */
  showdividers?: boolean;
  /**
   * Multicategory divider color. Defaults to `color`.
   */
  dividercolor?: string;
  /**
   * Multicategory divider width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  dividerwidth?: number;
  /**
   * Draw the axis line.
   *
   * @defaultValue `false`
   */
  showline?: boolean;
  /**
   * Axis line color. Defaults to `color`.
   */
  linecolor?: string;
  /**
   * Axis line width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  linewidth?: number;
  /**
   * Repeat the axis line on the opposite side of the plot area: `true` (line), `ticks` (line and ticks); `all`/`allticks` do the same on every subplot sharing this axis.
   *
   * @defaultValue `false`
   */
  mirror?: true | 'ticks' | false | 'all' | 'allticks';
  /**
   * Draw tick marks outside or inside the plot area, or not at all (`''`).
   *
   * @defaultValue `""`
   */
  ticks?: 'outside' | 'inside' | '';
  /**
   * Tick length in px.
   *
   * Minimum: 0
   *
   * @defaultValue `5`
   */
  ticklen?: number;
  /**
   * Tick width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  tickwidth?: number;
  /**
   * Tick color. Defaults to `color`.
   */
  tickcolor?: string;
  /**
   * Draw grid lines at each tick.
   *
   * @defaultValue `true`
   */
  showgrid?: boolean;
  /**
   * Grid line color.
   *
   * @defaultValue `"#eee"`
   */
  gridcolor?: string;
  /**
   * Grid line width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  gridwidth?: number;
  /**
   * Dash style: `solid`, `dot`, `dash`, `longdash`, `dashdot`, `longdashdot`, or a CSS-like list of dash lengths in px (`5px,10px,2px,2px`).
   *
   * @defaultValue `"solid"`
   */
  griddash?: string;
  /**
   * Draw a line at 0 (linear axes whose range contains 0).
   *
   * @defaultValue `true`
   */
  zeroline?: boolean;
  /**
   * Zero line color. Defaults to `color`.
   */
  zerolinecolor?: string;
  /**
   * Zero line width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  zerolinewidth?: number;
  /**
   * Minor ticks and grid lines between the major ticks.
   */
  minor?: LayoutXaxisMinor;
  /**
   * Axis title.
   */
  title?: LayoutXaxisTitle;
}

/**
 * Constraints applied to the computed autorange.
 */
export interface LayoutXaxisAutorangeoptions {
  /**
   * The autoranged minimum is exactly this value (data units).
   */
  minallowed?: unknown;
  /**
   * The autoranged maximum is exactly this value (data units).
   */
  maxallowed?: unknown;
  /**
   * The autoranged minimum is at least this value (data units).
   */
  clipmin?: unknown;
  /**
   * The autoranged maximum is at most this value (data units).
   */
  clipmax?: unknown;
  /**
   * Value(s) the autorange must include (data units).
   */
  include?: unknown;
}

/**
 * Tick-label font. Defaults to `layout.font`.
 */
export interface LayoutXaxisTickfont {
  /**
   * CSS font-family list. The renderer uses the first family it can load and falls back through the list.
   */
  family?: string;
  /**
   * Font size in CSS pixels.
   *
   * Minimum: 1
   */
  size?: number;
  /**
   * Text color.
   */
  color?: string;
  /**
   * Font weight: a CSS numeric weight (1–1000), `normal` or `bold`.
   *
   * Range: 1 – 1000
   */
  weight?: number | 'normal' | 'bold';
  /**
   * Font style.
   */
  style?: 'normal' | 'italic';
  /**
   * Capitals variant (CSS `font-variant-caps`). The SDF text renderer approximates small and petite caps with uppercase letters at a reduced size.
   */
  variant?:
    'normal' | 'small-caps' | 'all-small-caps' | 'all-petite-caps' | 'petite-caps' | 'unicase';
  /**
   * Letter case transform: `upper`, `lower`, `word caps` (first letter of each word), or `normal`.
   */
  textcase?: 'normal' | 'word caps' | 'upper' | 'lower';
  /**
   * Text decoration: `under`, `over` and/or `through` joined with `+` (e.g. `under+over`), or `none`.
   */
  lineposition?:
    'under' | 'over' | 'through' | `${'under' | 'over' | 'through'}+${string}` | 'none';
  /**
   * CSS `text-shadow` behind the text (`2px 2px 3px black`; only the first shadow is drawn), `none`, or `auto` for a thin halo in the contrast color of the text.
   */
  shadow?: string;
}

/**
 * One item of `tickformatstops`.
 *
 * Template defaults for every item go in `tickformatstopdefaults`.
 */
export interface LayoutXaxisTickformatstop {
  /**
   * Whether this stop is used.
   *
   * @defaultValue `true`
   */
  enabled?: boolean;
  /**
   * Set to `false` (by the template machinery) when `templateitemname` names no template stop; hidden stops are ignored like disabled ones.
   *
   * @defaultValue `true`
   */
  visible?: boolean;
  /**
   * Range `[min, max]` of the tick step (`dtick`) this format applies to; `null` for an open end. Date steps are in ms or `M<n>`.
   */
  dtickrange?: readonly [unknown, unknown];
  /**
   * The `tickformat` to use while the tick step is inside `dtickrange`.
   *
   * @defaultValue `""`
   */
  value?: string;
  /**
   * Name of this tickformatstop, used to reference it from a template via `templateitemname`.
   */
  name?: string;
  /**
   * Name of a template tickformatstop to inherit from. If no template item matches, this tickformatstop is hidden (`visible: false`).
   */
  templateitemname?: string;
}

/**
 * Minor ticks and grid lines between the major ticks.
 */
export interface LayoutXaxisMinor {
  /**
   * As the major `tickmode`. Defaults to `array` when `minor.tickvals` is set, `linear` when `minor.dtick` is set, else `auto`.
   */
  tickmode?: 'auto' | 'linear' | 'array';
  /**
   * Maximum number of minor ticks per major interval for `tickmode: auto` (0 picks it from the axis length).
   *
   * Minimum: 0
   *
   * @defaultValue `5`
   */
  nticks?: number;
  /**
   * As the major `tick0`. Defaults to the major `tick0`.
   */
  tick0?: unknown;
  /**
   * Minor tick step. Tick step. Linear axes: a positive number. Log axes: an integer `n` (a tick every `n` decades), `L<f>` (linear steps of `f` in data units, e.g. `L0.5`), `D1` (every digit) or `D2` (1, 2 and 5). Date axes: milliseconds, or `M<n>` for `n` months (`M12` for years).
   */
  dtick?: unknown;
  /**
   * Minor tick positions for `tickmode: array`.
   */
  tickvals?: DataArray;
  /**
   * Draw minor tick marks outside or inside the plot area, or not at all.
   *
   * @defaultValue `""`
   */
  ticks?: 'outside' | 'inside' | '';
  /**
   * Minor tick length in px. Defaults to 60% of `ticklen`.
   *
   * Minimum: 0
   */
  ticklen?: number;
  /**
   * Minor tick width in px. Defaults to `tickwidth`.
   *
   * Minimum: 0
   */
  tickwidth?: number;
  /**
   * Minor tick color. Defaults to `tickcolor`.
   */
  tickcolor?: string;
  /**
   * Draw minor grid lines.
   *
   * @defaultValue `false`
   */
  showgrid?: boolean;
  /**
   * Minor grid color. Defaults to `gridcolor` blended halfway to the plot background.
   */
  gridcolor?: string;
  /**
   * Minor grid width in px. Defaults to `gridwidth`.
   *
   * Minimum: 0
   */
  gridwidth?: number;
  /**
   * Minor grid dash style. Defaults to `griddash`. Dash style: `solid`, `dot`, `dash`, `longdash`, `dashdot`, `longdashdot`, or a CSS-like list of dash lengths in px (`5px,10px,2px,2px`).
   */
  griddash?: string;
}

/**
 * Axis title.
 */
export interface LayoutXaxisTitle {
  /**
   * Axis title text (supports rich-text tags, E2.10).
   *
   * @defaultValue `""`
   */
  text?: string;
  /**
   * Axis title font. Defaults to `layout.font` with 1.2× size.
   */
  font?: LayoutXaxisTitleFont;
  /**
   * Distance in px between the tick labels and the title. By default it follows the tick labels automatically.
   *
   * Minimum: 0
   */
  standoff?: number;
}

/**
 * Axis title font. Defaults to `layout.font` with 1.2× size.
 */
export interface LayoutXaxisTitleFont {
  /**
   * CSS font-family list. The renderer uses the first family it can load and falls back through the list.
   */
  family?: string;
  /**
   * Font size in CSS pixels.
   *
   * Minimum: 1
   */
  size?: number;
  /**
   * Text color.
   */
  color?: string;
  /**
   * Font weight: a CSS numeric weight (1–1000), `normal` or `bold`.
   *
   * Range: 1 – 1000
   */
  weight?: number | 'normal' | 'bold';
  /**
   * Font style.
   */
  style?: 'normal' | 'italic';
  /**
   * Capitals variant (CSS `font-variant-caps`). The SDF text renderer approximates small and petite caps with uppercase letters at a reduced size.
   */
  variant?:
    'normal' | 'small-caps' | 'all-small-caps' | 'all-petite-caps' | 'petite-caps' | 'unicase';
  /**
   * Letter case transform: `upper`, `lower`, `word caps` (first letter of each word), or `normal`.
   */
  textcase?: 'normal' | 'word caps' | 'upper' | 'lower';
  /**
   * Text decoration: `under`, `over` and/or `through` joined with `+` (e.g. `under+over`), or `none`.
   */
  lineposition?:
    'under' | 'over' | 'through' | `${'under' | 'over' | 'through'}+${string}` | 'none';
  /**
   * CSS `text-shadow` behind the text (`2px 2px 3px black`; only the first shadow is drawn), `none`, or `auto` for a thin halo in the contrast color of the text.
   */
  shadow?: string;
}

/**
 * A y axis. `yaxis2`, `yaxis3`, … declare further y axes, referenced from traces as `'y2'`, `'y3'`, ….
 */
export interface LayoutYaxis {
  /**
   * Draw the axis (line, ticks, labels, grid and title). A hidden axis still maps data to pixels.
   *
   * @defaultValue `true`
   */
  visible?: boolean;
  /**
   * Base color of the axis: the default for `linecolor`, `tickcolor`, `zerolinecolor`, `dividercolor` and (when changed) the tick-label and title font colors.
   *
   * @defaultValue `"#444"`
   */
  color?: string;
  /**
   * Axis type. `'-'` detects the type from the data of the first trace on this axis (Plotly's rules: mostly dates → `date`; more than twice as many distinct strings as numbers → `category`; two-level `[[groups], [items]]` arrays → `multicategory`; otherwise `linear`).
   *
   * @defaultValue `"-"`
   */
  type?: '-' | 'linear' | 'log' | 'date' | 'category' | 'multicategory';
  /**
   * How type detection treats numeric strings: `convert types` counts `'12'` as a number; `strict` counts only real numbers, so numeric strings make a `category` axis.
   *
   * @defaultValue `"convert types"`
   */
  autotypenumbers?: 'convert types' | 'strict';
  /**
   * Whether the range is computed from the data. `reversed` autoranges with the axis flipped. `min`/`max` autorange only that end and take the other from `range` (`min reversed`/`max reversed` also flip the axis). Defaults to `false` when a full `range` is given, and to `min`/`max` when one end of `range` is `null`.
   *
   * @defaultValue `true`
   */
  autorange?: true | false | 'reversed' | 'min reversed' | 'max reversed' | 'min' | 'max';
  /**
   * Constraints applied to the computed autorange.
   */
  autorangeoptions?: LayoutYaxisAutorangeoptions;
  /**
   * Linear axes only. `tozero` extends the autorange to include 0; `nonnegative` keeps it ≥ 0.
   *
   * @defaultValue `"normal"`
   */
  rangemode?: 'normal' | 'tozero' | 'nonnegative';
  /**
   * Disables zoom and pan on this axis (drag, scroll, and modebar zoom leave its range alone).
   *
   * @defaultValue `false`
   */
  fixedrange?: boolean;
  /**
   * Visible range `[start, end]` in data units (numbers, dates or category names; exponents on log axes, as in Plotly). Setting it turns `autorange` off; `null` for one end autoranges that end only.
   */
  range?: readonly [unknown, unknown];
  /**
   * Lowest value zoom and pan may reach, in range units (like `range`: exponents on log axes). Also caps the autorange.
   */
  minallowed?: unknown;
  /**
   * Highest value zoom and pan may reach, in range units (like `range`: exponents on log axes). Also caps the autorange.
   */
  maxallowed?: unknown;
  /**
   * Fraction of the plot area `[start, end]` this axis spans.
   *
   * @defaultValue `[0,1]`
   */
  domain?: readonly [number, number];
  /**
   * The x axis this axis is drawn against, or `free`. Defaults to the x axis of the first subplot using this axis.
   *
   * @defaultValue `"x"`
   */
  anchor?: 'x' | `x${number}` | 'free';
  /**
   * Which side of the plot area the axis is drawn on.
   *
   * @defaultValue `"left"`
   */
  side?: 'left' | 'right';
  /**
   * Draw this axis over another y axis (`'y'`, `'y2'`, …), sharing its `domain` (this axis' own `domain` is ignored), e.g. a secondary y axis with `side: 'right'`. Unset (or `free`) by default. The target must exist and must not overlay another axis itself; otherwise this is ignored. Templates cannot set it, since it names specific axes. Zoom and pan do not yet move overlaid axes together (plan E3.9).
   *
   * @defaultValue `"y"`
   */
  overlaying?: 'y' | `y${number}` | 'free';
  /**
   * Position of the axis in paper coordinates (0–1 across the x direction). Only used when `anchor` is `free`.
   *
   * Range: 0 – 1
   *
   * @defaultValue `0`
   */
  position?: number;
  /**
   * Draw the axis line, ticks and labels above or below the traces. Grid lines are always below.
   *
   * @defaultValue `"above traces"`
   */
  layer?: 'above traces' | 'below traces';
  /**
   * Grow the figure margins so tick labels and the title fit (E4.2). `true` allows every direction; flags restrict it.
   *
   * @defaultValue `false`
   */
  automargin?:
    | 'height'
    | 'width'
    | 'left'
    | 'right'
    | 'top'
    | 'bottom'
    | `${'height' | 'width' | 'left' | 'right' | 'top' | 'bottom'}+${string}`
    | true
    | false;
  /**
   * `auto` picks up to `nticks` round ticks; `linear` places ticks from `tick0` every `dtick`; `array` uses `tickvals`/`ticktext`; `sync` aligns with the overlaid axis (reserved; behaves as `auto`). Defaults to `array` when `tickvals` is set, `linear` when `dtick` is set, else `auto`.
   */
  tickmode?: 'auto' | 'linear' | 'array' | 'sync';
  /**
   * Maximum number of ticks for `tickmode: auto` (the actual number may be lower so steps stay round). 0 picks it from the axis length.
   *
   * Minimum: 0
   *
   * @defaultValue `0`
   */
  nticks?: number;
  /**
   * Position of one tick for `tickmode: linear` (data units; a date on date axes, an exponent on log axes unless `dtick` is `L<f>`). Defaults to 0, or 2000-01-01 on date axes.
   */
  tick0?: unknown;
  /**
   * Tick step. Linear axes: a positive number. Log axes: an integer `n` (a tick every `n` decades), `L<f>` (linear steps of `f` in data units, e.g. `L0.5`), `D1` (every digit) or `D2` (1, 2 and 5). Date axes: milliseconds, or `M<n>` for `n` months (`M12` for years).
   */
  dtick?: unknown;
  /**
   * Tick positions for `tickmode: array` (data units).
   */
  tickvals?: DataArray;
  /**
   * Tick labels for `tickvals`. Missing entries are formatted as usual.
   */
  ticktext?: DataArray;
  /**
   * Show tick labels.
   *
   * @defaultValue `true`
   */
  showticklabels?: boolean;
  /**
   * Tick-label font. Defaults to `layout.font`.
   */
  tickfont?: LayoutYaxisTickfont;
  /**
   * Tick-label rotation in degrees. `auto` rotates x-axis labels by the first `autotickangles` entry that avoids overlap.
   *
   * @defaultValue `"auto"`
   */
  tickangle?: number | 'auto';
  /**
   * Candidate angles, in order of preference, for `tickangle: auto`.
   *
   * @defaultValue `[0,30,90]`
   */
  autotickangles?: readonly number[];
  /**
   * Tick-label format: a [d3-format](https://github.com/d3/d3-format) specifier for numbers (`.2f`, `~s`, `$,`) or a [d3-time-format](https://github.com/d3/d3-time-format) specifier for dates (`%b %Y`), plus `%{n}f` for `n` digits of fractional seconds and `%h` for the half year. Empty picks a format automatically.
   *
   * @defaultValue `""`
   */
  tickformat?: string;
  /**
   * Zoom-dependent tick formats: the first enabled stop whose `dtickrange` contains the current tick step replaces `tickformat`.
   */
  tickformatstops?: Array<LayoutYaxisTickformatstop>;
  /**
   * Format of this axis’ values in hover labels (same syntax as `tickformat`). Empty uses the tick format with extra precision.
   *
   * @defaultValue `""`
   */
  hoverformat?: string;
  /**
   * Text before each tick label.
   *
   * @defaultValue `""`
   */
  tickprefix?: string;
  /**
   * Which tick labels get `tickprefix`.
   *
   * @defaultValue `"all"`
   */
  showtickprefix?: 'all' | 'first' | 'last' | 'none';
  /**
   * Text after each tick label.
   *
   * @defaultValue `""`
   */
  ticksuffix?: string;
  /**
   * Which tick labels get `ticksuffix`.
   *
   * @defaultValue `"all"`
   */
  showticksuffix?: 'all' | 'first' | 'last' | 'none';
  /**
   * How large and small numbers are written: `none` (1000000), `e` (1e+6), `E` (1E+6), `power` (1×10⁶), `SI` (1M), `B` (like SI but 1B for 10⁹).
   *
   * @defaultValue `"B"`
   */
  exponentformat?: 'none' | 'e' | 'E' | 'power' | 'SI' | 'B';
  /**
   * Which tick labels show the exponent.
   *
   * @defaultValue `"all"`
   */
  showexponent?: 'all' | 'first' | 'last' | 'none';
  /**
   * Smallest decimal exponent written in exponent form (`SI`/`B` and exponent formats); smaller magnitudes are written out in full.
   *
   * Minimum: 0
   *
   * @defaultValue `3`
   */
  minexponent?: number;
  /**
   * Always separate thousands (1,000). By default only numbers with 5 or more integer digits are separated.
   *
   * @defaultValue `false`
   */
  separatethousands?: boolean;
  /**
   * Date axes: `instant` labels the tick instant; `period` centers each label in the period that starts at its tick (e.g. month names between month ticks).
   *
   * @defaultValue `"instant"`
   */
  ticklabelmode?: 'instant' | 'period';
  /**
   * Tick labels outside or inside the plot area, optionally shifted to one side of the tick (`top`/`bottom` for y axes, `left`/`right` for x axes).
   *
   * @defaultValue `"outside"`
   */
  ticklabelposition?:
    | 'outside'
    | 'inside'
    | 'outside top'
    | 'inside top'
    | 'outside left'
    | 'inside left'
    | 'outside right'
    | 'inside right'
    | 'outside bottom'
    | 'inside bottom';
  /**
   * What to do with labels that overflow: keep them, or hide those past the figure or the axis domain. Defaults to `hide past div` for outside labels and `hide past domain` for inside labels.
   */
  ticklabeloverflow?: 'allow' | 'hide past div' | 'hide past domain';
  /**
   * Label every n-th tick only (`tickmode` `auto` and `linear`).
   *
   * Minimum: 1
   *
   * @defaultValue `1`
   */
  ticklabelstep?: number;
  /**
   * Shift of tick labels along the axis, in px.
   *
   * @defaultValue `0`
   */
  ticklabelshift?: number;
  /**
   * Extra distance between tick labels and ticks, in px.
   *
   * @defaultValue `0`
   */
  ticklabelstandoff?: number;
  /**
   * Order of categories: as they first appear in the traces (`trace`), alphabetical/numerical (`category ascending|descending`), `categoryarray` first (`array`), or by an aggregate of the trace values per category (`total`, `min`, `max`, `sum`, `mean`, `median`). Defaults to `array` when `categoryarray` is set, else `trace`.
   */
  categoryorder?:
    | 'trace'
    | 'category ascending'
    | 'category descending'
    | 'array'
    | 'total ascending'
    | 'total descending'
    | 'min ascending'
    | 'min descending'
    | 'max ascending'
    | 'max descending'
    | 'sum ascending'
    | 'sum descending'
    | 'mean ascending'
    | 'mean descending'
    | 'median ascending'
    | 'median descending';
  /**
   * Category order for `categoryorder: array`. Categories not listed follow in trace order.
   */
  categoryarray?: DataArray;
  /**
   * Multicategory axes: draw divider lines between groups.
   *
   * @defaultValue `true`
   */
  showdividers?: boolean;
  /**
   * Multicategory divider color. Defaults to `color`.
   */
  dividercolor?: string;
  /**
   * Multicategory divider width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  dividerwidth?: number;
  /**
   * Draw the axis line.
   *
   * @defaultValue `false`
   */
  showline?: boolean;
  /**
   * Axis line color. Defaults to `color`.
   */
  linecolor?: string;
  /**
   * Axis line width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  linewidth?: number;
  /**
   * Repeat the axis line on the opposite side of the plot area: `true` (line), `ticks` (line and ticks); `all`/`allticks` do the same on every subplot sharing this axis.
   *
   * @defaultValue `false`
   */
  mirror?: true | 'ticks' | false | 'all' | 'allticks';
  /**
   * Draw tick marks outside or inside the plot area, or not at all (`''`).
   *
   * @defaultValue `""`
   */
  ticks?: 'outside' | 'inside' | '';
  /**
   * Tick length in px.
   *
   * Minimum: 0
   *
   * @defaultValue `5`
   */
  ticklen?: number;
  /**
   * Tick width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  tickwidth?: number;
  /**
   * Tick color. Defaults to `color`.
   */
  tickcolor?: string;
  /**
   * Draw grid lines at each tick.
   *
   * @defaultValue `true`
   */
  showgrid?: boolean;
  /**
   * Grid line color.
   *
   * @defaultValue `"#eee"`
   */
  gridcolor?: string;
  /**
   * Grid line width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  gridwidth?: number;
  /**
   * Dash style: `solid`, `dot`, `dash`, `longdash`, `dashdot`, `longdashdot`, or a CSS-like list of dash lengths in px (`5px,10px,2px,2px`).
   *
   * @defaultValue `"solid"`
   */
  griddash?: string;
  /**
   * Draw a line at 0 (linear axes whose range contains 0).
   *
   * @defaultValue `true`
   */
  zeroline?: boolean;
  /**
   * Zero line color. Defaults to `color`.
   */
  zerolinecolor?: string;
  /**
   * Zero line width in px.
   *
   * Minimum: 0
   *
   * @defaultValue `1`
   */
  zerolinewidth?: number;
  /**
   * Minor ticks and grid lines between the major ticks.
   */
  minor?: LayoutYaxisMinor;
  /**
   * Axis title.
   */
  title?: LayoutYaxisTitle;
}

/**
 * Constraints applied to the computed autorange.
 */
export interface LayoutYaxisAutorangeoptions {
  /**
   * The autoranged minimum is exactly this value (data units).
   */
  minallowed?: unknown;
  /**
   * The autoranged maximum is exactly this value (data units).
   */
  maxallowed?: unknown;
  /**
   * The autoranged minimum is at least this value (data units).
   */
  clipmin?: unknown;
  /**
   * The autoranged maximum is at most this value (data units).
   */
  clipmax?: unknown;
  /**
   * Value(s) the autorange must include (data units).
   */
  include?: unknown;
}

/**
 * Tick-label font. Defaults to `layout.font`.
 */
export interface LayoutYaxisTickfont {
  /**
   * CSS font-family list. The renderer uses the first family it can load and falls back through the list.
   */
  family?: string;
  /**
   * Font size in CSS pixels.
   *
   * Minimum: 1
   */
  size?: number;
  /**
   * Text color.
   */
  color?: string;
  /**
   * Font weight: a CSS numeric weight (1–1000), `normal` or `bold`.
   *
   * Range: 1 – 1000
   */
  weight?: number | 'normal' | 'bold';
  /**
   * Font style.
   */
  style?: 'normal' | 'italic';
  /**
   * Capitals variant (CSS `font-variant-caps`). The SDF text renderer approximates small and petite caps with uppercase letters at a reduced size.
   */
  variant?:
    'normal' | 'small-caps' | 'all-small-caps' | 'all-petite-caps' | 'petite-caps' | 'unicase';
  /**
   * Letter case transform: `upper`, `lower`, `word caps` (first letter of each word), or `normal`.
   */
  textcase?: 'normal' | 'word caps' | 'upper' | 'lower';
  /**
   * Text decoration: `under`, `over` and/or `through` joined with `+` (e.g. `under+over`), or `none`.
   */
  lineposition?:
    'under' | 'over' | 'through' | `${'under' | 'over' | 'through'}+${string}` | 'none';
  /**
   * CSS `text-shadow` behind the text (`2px 2px 3px black`; only the first shadow is drawn), `none`, or `auto` for a thin halo in the contrast color of the text.
   */
  shadow?: string;
}

/**
 * One item of `tickformatstops`.
 *
 * Template defaults for every item go in `tickformatstopdefaults`.
 */
export interface LayoutYaxisTickformatstop {
  /**
   * Whether this stop is used.
   *
   * @defaultValue `true`
   */
  enabled?: boolean;
  /**
   * Set to `false` (by the template machinery) when `templateitemname` names no template stop; hidden stops are ignored like disabled ones.
   *
   * @defaultValue `true`
   */
  visible?: boolean;
  /**
   * Range `[min, max]` of the tick step (`dtick`) this format applies to; `null` for an open end. Date steps are in ms or `M<n>`.
   */
  dtickrange?: readonly [unknown, unknown];
  /**
   * The `tickformat` to use while the tick step is inside `dtickrange`.
   *
   * @defaultValue `""`
   */
  value?: string;
  /**
   * Name of this tickformatstop, used to reference it from a template via `templateitemname`.
   */
  name?: string;
  /**
   * Name of a template tickformatstop to inherit from. If no template item matches, this tickformatstop is hidden (`visible: false`).
   */
  templateitemname?: string;
}

/**
 * Minor ticks and grid lines between the major ticks.
 */
export interface LayoutYaxisMinor {
  /**
   * As the major `tickmode`. Defaults to `array` when `minor.tickvals` is set, `linear` when `minor.dtick` is set, else `auto`.
   */
  tickmode?: 'auto' | 'linear' | 'array';
  /**
   * Maximum number of minor ticks per major interval for `tickmode: auto` (0 picks it from the axis length).
   *
   * Minimum: 0
   *
   * @defaultValue `5`
   */
  nticks?: number;
  /**
   * As the major `tick0`. Defaults to the major `tick0`.
   */
  tick0?: unknown;
  /**
   * Minor tick step. Tick step. Linear axes: a positive number. Log axes: an integer `n` (a tick every `n` decades), `L<f>` (linear steps of `f` in data units, e.g. `L0.5`), `D1` (every digit) or `D2` (1, 2 and 5). Date axes: milliseconds, or `M<n>` for `n` months (`M12` for years).
   */
  dtick?: unknown;
  /**
   * Minor tick positions for `tickmode: array`.
   */
  tickvals?: DataArray;
  /**
   * Draw minor tick marks outside or inside the plot area, or not at all.
   *
   * @defaultValue `""`
   */
  ticks?: 'outside' | 'inside' | '';
  /**
   * Minor tick length in px. Defaults to 60% of `ticklen`.
   *
   * Minimum: 0
   */
  ticklen?: number;
  /**
   * Minor tick width in px. Defaults to `tickwidth`.
   *
   * Minimum: 0
   */
  tickwidth?: number;
  /**
   * Minor tick color. Defaults to `tickcolor`.
   */
  tickcolor?: string;
  /**
   * Draw minor grid lines.
   *
   * @defaultValue `false`
   */
  showgrid?: boolean;
  /**
   * Minor grid color. Defaults to `gridcolor` blended halfway to the plot background.
   */
  gridcolor?: string;
  /**
   * Minor grid width in px. Defaults to `gridwidth`.
   *
   * Minimum: 0
   */
  gridwidth?: number;
  /**
   * Minor grid dash style. Defaults to `griddash`. Dash style: `solid`, `dot`, `dash`, `longdash`, `dashdot`, `longdashdot`, or a CSS-like list of dash lengths in px (`5px,10px,2px,2px`).
   */
  griddash?: string;
}

/**
 * Axis title.
 */
export interface LayoutYaxisTitle {
  /**
   * Axis title text (supports rich-text tags, E2.10).
   *
   * @defaultValue `""`
   */
  text?: string;
  /**
   * Axis title font. Defaults to `layout.font` with 1.2× size.
   */
  font?: LayoutYaxisTitleFont;
  /**
   * Distance in px between the tick labels and the title. By default it follows the tick labels automatically.
   *
   * Minimum: 0
   */
  standoff?: number;
}

/**
 * Axis title font. Defaults to `layout.font` with 1.2× size.
 */
export interface LayoutYaxisTitleFont {
  /**
   * CSS font-family list. The renderer uses the first family it can load and falls back through the list.
   */
  family?: string;
  /**
   * Font size in CSS pixels.
   *
   * Minimum: 1
   */
  size?: number;
  /**
   * Text color.
   */
  color?: string;
  /**
   * Font weight: a CSS numeric weight (1–1000), `normal` or `bold`.
   *
   * Range: 1 – 1000
   */
  weight?: number | 'normal' | 'bold';
  /**
   * Font style.
   */
  style?: 'normal' | 'italic';
  /**
   * Capitals variant (CSS `font-variant-caps`). The SDF text renderer approximates small and petite caps with uppercase letters at a reduced size.
   */
  variant?:
    'normal' | 'small-caps' | 'all-small-caps' | 'all-petite-caps' | 'petite-caps' | 'unicase';
  /**
   * Letter case transform: `upper`, `lower`, `word caps` (first letter of each word), or `normal`.
   */
  textcase?: 'normal' | 'word caps' | 'upper' | 'lower';
  /**
   * Text decoration: `under`, `over` and/or `through` joined with `+` (e.g. `under+over`), or `none`.
   */
  lineposition?:
    'under' | 'over' | 'through' | `${'under' | 'over' | 'through'}+${string}` | 'none';
  /**
   * CSS `text-shadow` behind the text (`2px 2px 3px black`; only the first shadow is drawn), `none`, or `auto` for a thin halo in the contrast color of the text.
   */
  shadow?: string;
}
