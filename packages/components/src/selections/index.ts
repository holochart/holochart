/**
 * Selections as layout objects (plan E5.12): the component. The view, with the outline geometry
 * (`geometry.ts`), loads on first use (`shared/lazy-view.ts`).
 */
export { selectionsComponent } from './selections.ts';
export type { SelectionDragMode, SelectionOutline } from './geometry.ts';
