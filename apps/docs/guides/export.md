---
title: Exporting images & vectors
description: Export charts as PNG, JPEG, or WebP images, and as SVG or PDF later.
status: stub
milestone: M2
---

# Exporting images & vectors

This guide will cover exporting charts as images and data.

Planned topics:

- `chart.toImage({ format, width, height, scale })` for PNG, JPEG, and WebP
- The modebar download button and `config.toImageButtonOptions`
- High-resolution export and effects during export
- Vector export (SVG, PDF) for 2D charts (planned for M7)
- Server-side and headless image generation (planned)
- Exporting the figure as JSON
