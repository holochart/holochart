---
title: Configuration options
description: Use the config object to control per-chart behavior such as responsiveness, zoom, and the modebar.
status: stub
milestone: M1
---

# Configuration options

This page will explain the `config` object: settings that control how a chart behaves, not how it
looks.

Planned topics:

- `responsive` and container sizing
- Interaction settings: `scrollZoom`, `doubleClick`, `staticPlot`, `editable`
- Modebar settings: `displayModeBar`, adding and removing buttons
- Rendering settings: `pixelRatio`, `antialias`, `powerPreference`, `textRenderer`
- `locale` and `strict` validation

The full list of options is in the [config reference](/reference/config).
