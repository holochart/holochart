---
'@mk7s/holochart-components': minor
'@mk7s/holochart-render': minor
'@mk7s/holochart-runtime': minor
'@mk7s/holochart': minor
---

Shapes (E5.5): lines, rectangles, circles and SVG paths in data, paper or domain coordinates, with labels, layers, drag/resize editing, the `addHline`/`addVline`/`addHrect`/`addVrect` helpers, and autorange that includes data-referenced shapes. Layout images (E5.6) with `contain`/`fill`/`stretch` sizing.
