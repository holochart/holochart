---
'@mk7s/holochart-traces-stats': minor
'@mk7s/holochart-core': minor
'@mk7s/holochart': minor
---

`parcoords` (GPU-drawn lines, axis brushing with multiple ranges and `restyle` like Plotly, axis reordering) and `parcats` (category bands and ribbons, `hspline` paths, drag to reorder categories and dimensions, hover highlighting).
