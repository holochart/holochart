---
'@mk7s/holochart-core': minor
'@mk7s/holochart-runtime': minor
'@mk7s/holochart-components': minor
'@mk7s/holochart-traces-basic': minor
'@mk7s/holochart': minor
---

Interaction: legend clicks toggle the whole `legendgroup` (including `showlegend: false` traces) as in Plotly; shape drawing with the `drawline`, `drawopenpath`, `drawclosedpath`, `drawcircle`, `drawrect` dragmodes and `eraseshape`, with `newshape`/`activeshape` defaults; the modebar shows on tap on touch devices; scatter fills hover in every hovermode; `crossTraceCalc` can report which traces changed, so stacked streaming keeps its fast path.
