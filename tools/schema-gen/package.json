{
  "name": "@mk7s/holochart-schema-gen",
  "private": true,
  "type": "module",
  "description": "Generates TS types, JSON schema, and attribute reference docs from the attribute schema (plan E1.2, E19.3).",
  "scripts": {
    "gen": "node --conditions=source src/cli.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@mk7s/holochart-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "catalog:",
    "prettier": "~3.9.8"
  }
}
